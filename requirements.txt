# SyntaxAI — core runtime dependencies (always required)
# Install:  pip install -r requirements.txt
# Optional groups live in requirements-web.txt / requirements-gemini.txt
# or via the package extras:  pip install ".[web]" / ".[gemini]" / ".[dev]"
pyyaml>=6.0
httpx>=0.24.0
